{
  "name": "trench",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": "true",
      "username": "vscode"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y opam build-essential m4 pkg-config libgmp-dev git-lfs && opam init -a --disable-sandboxing --compiler=ocaml-base-compiler.5.1.1 && eval $(opam env) && opam install -y dune menhir && echo 'eval $(opam env --switch=ocaml-base-compiler.5.1.1 -s)' >> /home/vscode/.bashrc",
  "customizations": {
    "vscode": {
      "extensions": [
        "ocamllabs.ocaml-platform",
        "ms-vscode.cpptools"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
